"""
Django local settings for doi_site project.

This file contains the variables that MAY/SHOULD be set with values specific for
the deployment.
"""

# Datacite URL, the default in settings.py is the test service
# Override here to use the production service
# DATACITE_URL = 'https://mds.datacite.org/'

# Datacite handler, the default in settings.py is the test service
# Override here to use the production service
# DATACITE_HANDLER = 'http://dx.doi.org/'

# Web proxy
HTTP_PROXY_HOST = 'myproxy.example.com'
HTTP_PROXY_PORT = '8080'

# Organisation's DataCite prefix in the form nn.nnnn
DOI_PREFIX = 'nn.nnnn'

# Datacite username
DATACITE_USER_NAME = 'BL.XXXX'

# Datacite password
DATACITE_PASSWORD = 'xxxxxxxxxxxxxxxxx'

# Organisations LDAP server
AUTH_LDAP_SERVER_URI = "ldap://myldap.example.com:389"

# Organisations LDAP DN template
AUTH_LDAP_USER_DN_TEMPLATE = "%(user)s@myldap.example.com"

# The name of your organisation to display on the home page
ORGANISATION_NAME = 'My Organisation'

# The email address to display on the home page
ORGANISATION_DOI_EMAIL = 'doi@smyorganisation.example.com'

# The URL of the location of the document detailing users roles and responsibilities
ROLES_URL = 'https://example.com/DOI-responsibilities.docx'

# The URL of the location of the document containing notes for issuers
NOTES_URL = 'https://example.com/Notes%20for%20Issuers.docx'

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

# A list of strings representing the host/domain names that this Django site can
# serve
ALLOWED_HOSTS = ['127.0.0.1']

# A sample logging configuration. The only tangible logging
# performed by this configuration is to send an email to
# the site admins on every HTTP 500 error.
# See http://docs.djangoproject.com/en/dev/topics/logging for
# more details on how to customize your logging configuration.
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'formatters': {
        'verbose': {
            'format':
            '%(levelname)s %(asctime)s %(module)s %(process)d %(thread)d %(message)s'
        },
        'simple': {
            'format': '%(levelname)s %(message)s'
        },
    },
    'handlers': {
        'console':{
            'level':'DEBUG',
            'class':'logging.StreamHandler',
            'formatter': 'verbose'
        }
    },
    'loggers': {
        'mds': {
            'handlers': ['console'],
            'level': 'DEBUG',
            'propagate': True,
        }
    }
}

DATABASES = { 'default': { 'ENGINE': 'django.db.backends.sqlite3', 'NAME': '/var/doi/doi.db' } }

